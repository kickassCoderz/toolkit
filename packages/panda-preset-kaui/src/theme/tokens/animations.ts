import { defineTokens } from "@pandacss/dev";

export const animations = defineTokens.animations({
    "skeleton-pulse": {
        value: "skeleton-pulse 1000ms infinite alternate-reverse",
    },
});
